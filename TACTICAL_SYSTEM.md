# تنفيذ خطة منطق التشكيلات والحركة الواقعية

هذا المستند يشرح كيف نُفِّذت كل نقطة من نقاط ملف «خطة_منطق_الحركة_الواقعية.txt» داخل المشروع، وأين يجدها المطوِّر في الكود.

## الملفات الجديدة

| الملف | الغرض |
| --- | --- |
| `lib/src/game/tactics/tactical_engine.dart` | قلب النظام: يبني «السياق التكتيكي» لكل فريق في كل إطار زمني ويحسب الهدف الديناميكي لكل لاعب وفق تسلسل الخطة (البند 29). |
| `lib/src/game/tactics/team_play_state.dart` | حالات الفريق الخمس (البند 3) + عوامل الاستعجال والتماسك وأولوية الضغط لكل دور. |
| `lib/src/game/tactics/team_shape_kind.dart` | أشكال الفريق الخمسة: الأساسي/الهجومي/الدفاعي/الضاغط/الانتقالي (البندان 4 و24). |
| `lib/src/game/tactics/danger_zone.dart` | مناطق الخطورة: آمنة/متوسطة/خطرة/حرجة (البند 13). |
| `lib/src/game/models/tactics/shape_profile.dart` | ملف السلوك الخاص بكل تشكيلة (مقدار تقدّم الخط، عمق الكتلة، العرض، هل تهبط الأجنحة، التبادل الداخلي للـ 4-6-0). |

## الملفات المعدَّلة

| الملف | التعديل |
| --- | --- |
| `lib/src/game/enums/player_role.dart` | أدوار مستقلة عن عدد الخطوط: `GK, LB, RB, CB1, CB2, LIB, LWB, RWB, DM, CM1, CM2, AM, LW, RW, ST` (البند 19)، مع بيانات سلوكية لكل دور: درجة الحرية (البند 21)، نطاق المنطقة «Zone» (البند 2)، الميل نحو جهة الكرة (البند 7)، أولوية الضغط، عامل مساعدة الدفاع (البند 11)، وبديل السرعة (البند 22). |
| `lib/src/game/models/formation.dart` | كل التشكيلات الـ23 أصبحت «Formation Preset» بأدوار صريحة (لا استنتاج من عدد الخطوط)، ولكل تشكيلة ملف شكل سلوكي (البندان 4 و24). أمثلة الخطة مطبقة حرفياً: 4-3-3 (CM-DM-CM / LW-ST-RW مع تحول دفاعي إلى 4-5-1 وهجومي بإكمال الظهيرين)، 4-2-4 (أربعة مهاجمين وأجنحة يعودان عند الفقد)، 4-6-0 (بلا مهاجم ثابت ومنطقة المهاجم سلوك متبادل). |
| `lib/src/game/logic/match_engine.dart` | آلة حالات الفريق (تتبع الاستحواذ ونوافذ الانتقال)، إعادة بناء السياق التكتيكي كل إطار، خط الدفاع الديناميكي، خط التسلل، مستوى المخاطرة حسب النتيجة والوقت، تمديد نزول المهاجمين عند حماية النتيجة، تحديث مسار الفريق المُدار آلياً ليعمل بنفس الخط التكتيكي، وتحديث نظام التغطية ليعتمد المرتكز الديناميكي بدل الثابت. |
| `lib/src/game/logic/player_ai.dart` | إعادة كتابة الحركة بلا كرة وفق سلسلة الأولويات: حماية المرمى ← إيقاف الخطر ← تغطية الضاغط ← إغلاق الممر ← الضغط ← العودة للشكل (البندان 14 و23)، دفع الخصم بعيداً عن المرمى (البند 12)، قاعدة «لا يركض الجميع خلف الكرة» (البند 6)، سلوكات الأدوار (البند 20)، وسرعة محسوبة من الحالة والمسافة والخطر والتعب والدور (البند 22). |
| `lib/src/game/enums/ai_play_style.dart` | إضافة عاملَي `compactnessBias` (تماسك الكتلة، البند 5) و`counterPressIntensity` (قوة الضغط العكسي، البند 16)، مع ربط بقية العوامل الموجودة بخط الأنابيب. |

## تسلسل المحرك (البند 29) وأين يتحقق

```
FORMATION            → formationPlan(type).spots (أدوار صريحة لكل تشكيلة)
PLAYER ROLES         → PlayerRole + بيانات الدور السلوكية
BASE POSITIONS       → TacticalContext.baseAnchor
TEAM STATE           → MatchEngine.currentPlayState (5 حالات)
BALL LOCATION        → ميل الكتلة نحو الكرة داخل dynamicAnchor
OPPONENT LOCATION    → خط الدفاع defensiveLineX + خط التسلل offsideLineX + مراقبة المهاجم
TEAM COMPACTNESS    → measureCompactness / targetCompactnessFor + تصحيح داخل dynamicAnchor
TACTICAL STYLE       → AiPlayStyle (عرض، ارتفاع خط، ضغط، تماسك، مخاطرة، إيقاع)
SCORE + TIME         → scoreUrgencyFor (متأخر/متقدم، الدقائق الأخيرة)
RISK LEVEL           → riskLevelFor
PLAYER PRIORITY      → سلسلة القرار الدفاعية/الهجومية في PlayerAi
DYNAMIC TARGET       → TacticalContext.dynamicAnchor
PLAYER MOVEMENT      → PlayerAi._movementForce + engine.moveTowards
```

تتكرر العملية كل إطار زمني (`_tickPlayStates` في بداية `tick`)، فلا تبقى اللاعبون في نقاط ثابتة (البند 28).

## قرارات مفتاحية

- **التشكيلة تحدد نقطة البداية فقط**: المرتكز `Anchor` يتشوه حسب الشكل الحالي (هجومي/دفاعي/ضاغط/انتقالي) بمعاملات مستمرة، ثم تنحرف الكتلة نحو الكرة والخصم.
- **لا قيم إزاحة ثابتة**: كل الإزاحات ناتج دوال متصلة (نسبة تقدم الكرة، ثقل منطقة الخطورة، خط التسلل…)، وسرعة الجري قوة محسوبة تتأثر بالحالة والمسافة والتعب والدور.
- **التماسك ككتلة**: يُقاس تباعد الخطوط وعرض الكتلة فعلياً ويُقارن بالمستهدف؛ إذا استُطالت الكتلة انقبضت وإذا انقبضت أثناء الاستحواذ توسعت (البند 5).
- **الضغط بلا فوضى**: ضاغط واحد معيَّن (`designatedPresser`) حسب الدور والمسافة وحالة الفريق، لاعب يغطيه من الجهة الخلفية، والوسط يغلق ممرات التمرير نحو المناطق الخطرة (البندان 6 و14 و15).
- **الانتقالات**: بعد فقدان الكرة نافذة ضغط عكسي مدتها بحسب أسلوب الفريق، وبعدها دفاع منظم؛ بعد كسبها هجوم سريع إذا وُجدت مساحة خلف دفاع الخصم (البند 16).

## الاختبارات

`test/tactical_system_test.dart` يغطي: صحة التشكيلات الـ23 والأدوار الصريحة (19)، أمثلة الخطة الثلاثة (25، 26، 27)، مناطق الخطورة (13)، الحالات والانتقالات (3، 16)، خط الدفاع (9)، خط التسلل (10)، التماسك (5)، المخاطرة حسب النتيجة والوقت (17)، بيانات الحرية (21)، ميلان الهدف الديناميكي نحو الكرة (7، 22، 28)، تحول 4-5-1، والثعلب الزائف في 4-6-0، إضافة إلى محاكاة كاملة تضمن بقاء الجميع داخل الملعب.
